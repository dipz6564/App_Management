namespace TaskManagementEngine
{
    public enum MainMenuOption
    {
        AddTask = 1,
        EditTask,
        DeleteTask,
        ViewTaskDetails,
        AddColumn,
        MoveTask,
        SortTasks,
        Exit,
        Invalid
    }

    // Add more enums if needed for other purposes in the application
}
